#!/usr/bin/env bash
set -e

echo "pre-commit: running make lint..."
make lint

echo "pre-commit: running make typecheck..."
make typecheck

echo "pre-commit: running make test-all..."
make test-all

echo "pre-commit: checking docs are up to date..."
scripts/check-docs.sh

echo "pre-commit: all checks passed."
