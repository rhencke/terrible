#!/usr/bin/env python3
"""Thin wrapper executable for running the terrible provider."""
import runpy
import sys

if __name__ == "__main__":
    # Run the package's CLI entrypoint
    sys.exit(runpy.run_module("terrible_provider.cli", run_name="__main__"))
