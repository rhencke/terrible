---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "terrible_community_clickhouse_clickhouse_quota Data Source - terrible"
subcategory: ""
description: |-
  
---

# terrible_community_clickhouse_clickhouse_quota (Data Source)





<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `host_id` (String) ID of the `terrible_host` to run this data source against
- `name` (String) Quota name to add or remove.

### Optional

- `apply_to` (String) Apply this quota to the following list of users/roles dependent on `apply_to_mode`. Can include special keywords of default and current_user or the name of an actual user or role. Is an error to specify this if `apply_to_mode=all`.
- `apply_to_mode` (String) When `listed_only` (default), the quota will only apply to the users/roles specified in `apply_to`. When `all`, the quota will only apply to **all** users/roles. When `all_except_listed`, the quota will only apply to **all** the users/roles except those specified in `apply_to`.
- `cluster` (String) Run the command on all cluster hosts. If the cluster is not configured, the command will crash with an error.
- `keyed_by` (String) Keys the quota by the specified key (default is to not key).
- `limits` (String) The limits that this quota should enforce.
- `state` (String) Quota state. `present` creates the quota if it does not exist. `absent` deletes the quota if it exists.

### Read-Only

- `executed_statements` (String) Data-modifying executed statements.
